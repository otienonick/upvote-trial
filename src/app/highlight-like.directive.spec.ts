import { HighlightLikeDirective } from './highlight-like.directive';

describe('HighlightLikeDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLikeDirective();
    expect(directive).toBeTruthy();
  });
});
